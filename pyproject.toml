[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "preverus"
version = "0.1.2"
description = "Python client for Preverus fraud decisions, events, lookups, and webhooks."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Preverus" }]
keywords = ["preverus", "fraud", "risk", "fingerprint", "laravel", "django", "flask"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]

[project.urls]
Homepage = "https://preverus.com"
Documentation = "https://preverus.com/docs"
Source = "https://github.com/preverus/preverus-python"
Issues = "https://github.com/preverus/preverus-python/issues"

[tool.setuptools.packages.find]
where = ["src"]
